// Prevents typo bugs from scattered string literals like 'accessToken' vs 'access_token'
export const STORAGE_KEYS = {
  USER: 'r777_user', // only non-sensitive display data, if you ever cache anything
};